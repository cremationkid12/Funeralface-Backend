# App
NODE_ENV=development
PORT=8010

# Security — HS256 test/dev tokens only (e.g. API tests). Sign with jsonwebtoken using claims sub, org_id, role.
# Production/mobile: Supabase access tokens are ES256; requireAuth validates them via Supabase getUser + staff_members lookup.
# JWT_SECRET is not used to verify Supabase user JWTs. Keep set for local tests that mock HS256 Bearer tokens.
JWT_SECRET=local-test-secret-only

# Database
# Local default for development only. Use your Supabase Postgres URL in staging/production.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/everroute

# Supabase (server-side auth provider; service role must never ship to mobile)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=replace-with-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
SUPABASE_STORAGE_BUCKET=everroute-assets
UPLOAD_IMAGE_MAX_BYTES=5242880

# Staff invite email (SendGrid)
SENDGRID_API_KEY=SG.xxxxx
INVITE_FROM_EMAIL=noreply@everroutefuneral.com
INVITE_FROM_NAME=EverRoute Funeral
# URL where invited staff lands first. If path ends with `/invite`,
# backend appends the invite token path segment automatically.
# Example: https://everroutefuneral.com/invite/<token>
INVITE_SIGNUP_URL=http://localhost:3000/invite

# CORS — comma-separated exact origins (scheme + host + port). Browsers send Origin on cross-site requests; preflight OPTIONS must get Access-Control-Allow-Origin for that exact string.
# Local dev:
ALLOWED_ORIGINS=http://localhost:8010,http://localhost:8080,http://localhost:5173
# Production (Railway): set the same variable in the service dashboard — include every origin your web app uses, e.g.:
# ALLOWED_ORIGINS=https://www.everroutefuneral.com,https://everroutefuneral.com

# API docs (Swagger UI at /docs). Set ENABLE_SWAGGER_UI=false to hide in production.
# SWAGGER_SERVER_URL=http://localhost:8010
